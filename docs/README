To generate the API documentation, run this in the Troy root directory:

Epydoc:

  epydoc -v --config=docs/epydoc.troy.pilot.cfg



Sphinx:

  sphinx-apidoc -F -o docs troy/
  cd docs/
  ln -s ../troy .
  make html
  

